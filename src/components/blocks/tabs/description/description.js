import React from 'react'

function Description (props) {
    console.log(props)
    return (
        <>
        {/* {description.text} 
        {description.price} {description.weight} */}
        </>
    )
}

export default Description